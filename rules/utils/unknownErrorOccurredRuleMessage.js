/* istanbul ignore next */
function unknownErrorOccurredRuleMessage(errorMessage) {
  /* istanbul ignore next */
  return `Unknown error occurred: '${errorMessage}'`;
}

module.exports = {
  unknownErrorOccurredRuleMessage
};
